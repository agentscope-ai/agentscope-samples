name: browser_use_fullstack_runtime_test
on:
  push:
    branches: [ main, lfh_samples2 ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.12']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug directory structure
        run: |
          # ✅ Show actual directory structure
          echo "Current directory: $(pwd)"
          ls -la

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          # ✅ Use correct relative path
          cd browser_use_fullstack_runtime/backend
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio

      - name: Run tests
        run: |
          # ✅ Use correct relative path
          cd browser_use_fullstack_runtime
          python -m pytest tests/browser_use_fullstack_runtime_test.py -v